// Import necessary modules and styles
import React from "react";
import "./nav.css";
import NavLinks from "./NavLinks"; // Import the 'NavLinks' component

// Define the 'DesktopNav' component
function DesktopNav() {
   return <NavLinks />; // Render the 'NavLinks' component
}

// Export the 'DesktopNav' component as the default export
export default DesktopNav;
